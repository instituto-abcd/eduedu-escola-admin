import { create } from "zustand";

export const useStudentFilterStore = create(() => ({}));
export const useSchoolClassFilterStore = create(() => ({}));
export const useUserFilterStore = create(() => ({}));
